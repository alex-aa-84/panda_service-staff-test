spring:
  application:
    name: service-staff-test
  config:
    enabled: true
    import: configserver:https://service-config.azurewebsites.net
  cloud:
    config:
      username: root
      password: P@nd@WWF_M
